name: meiku

services:
  meiku:
    image: ${MEIKU_IMAGE:-ghcr.io/maroishiku/meiku:v0.2.5@sha256:a80e2e12070598d0dcde8c2b7a130a626811243be40e4ed428726003363550fc}
    pull_policy: always
    container_name: meiku
    hostname: meiku
    restart: unless-stopped
    init: true
    stop_signal: SIGTERM
    stop_grace_period: 10s

    environment:
      TZ: ${TZ:-Europe/Berlin}
      ISHIKU_DATA_DIR: /data
      ISHIKU_LOG_LEVEL: ${ISHIKU_LOG_LEVEL:-info}
      ISHIKU_SETUP_SECRET: ${ISHIKU_SETUP_SECRET:-${DV2_SHARED_SECRET:-}}
      MEIKU_UID: "10001"
      MEIKU_GID: "10001"
      DV2_PORT: "8080"
      DV2_DATA_FILE: /data/data.json
      DV2_SHARED_SECRET: ${DV2_SHARED_SECRET:-${ISHIKU_SETUP_SECRET:-}}
      DV2_ACCESS_LOG: ${DV2_ACCESS_LOG:-false}

    ports:
      - target: 8080
        published: ${WEBUI_PORT:-65003}
        protocol: tcp

    volumes:
      - type: bind
        source: ${MEIKU_DATA_PATH:-${DATELLO_DATA_PATH:-/DATA/AppData/meiku/data}}
        target: /data

    read_only: true
    tmpfs:
      - /tmp:size=16m,mode=1777
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - SETGID
      - SETUID
    security_opt:
      - no-new-privileges:true
    pids_limit: 128
    mem_limit: 128m
    cpus: "0.50"

    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8080/readyz', timeout=3).read()"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s

    logging:
      driver: json-file
      options:
        max-size: "5m"
        max-file: "3"

    networks:
      - meiku

    labels:
      icon: "https://raw.githubusercontent.com/MaroIshiku/meiku/main/assets/icons/icon-512.png"
      org.opencontainers.image.title: "Meiku - Profile Share"
      org.opencontainers.image.description: "Private encrypted profile, contact and payment QR vault with local QR generation."
      org.opencontainers.image.source: "https://github.com/MaroIshiku/meiku"
      org.opencontainers.image.url: "https://github.com/MaroIshiku/meiku"

networks:
  meiku:
    name: meiku
    driver: bridge

x-casaos:
  architectures:
    - amd64
    - arm64
  main: meiku
  author: MaroIshiku
  developer: MaroIshiku
  category: Utilities
  title:
    en_us: Meiku
    de_de: Meiku
  tagline:
    en_us: Profile Share for encrypted contact and payment QR cards.
    de_de: Profile Share for encrypted contact and payment QR cards.
  description:
    en_us: Mobile-first PWA for encrypted private and business profile cards. The server stores only the encrypted token; QR codes and encryption run locally in the browser.
    de_de: Mobile-first PWA for encrypted private and business profile cards. The server stores only the encrypted token; QR codes and encryption run locally in the browser.
  icon: https://raw.githubusercontent.com/MaroIshiku/meiku/main/assets/icons/icon-512.png
  thumbnail: https://raw.githubusercontent.com/MaroIshiku/meiku/main/assets/icons/icon-512.png
  index: /
  port_map: ${WEBUI_PORT:-65003}
  tips:
    before_install:
      en_us: |
        Required: set ISHIKU_SETUP_SECRET to a long random value before starting. DV2_SHARED_SECRET remains supported as a legacy alias.

        Persistent data is stored on the ZimaOS host at /DATA/AppData/meiku/data and mounted into the container at /data. On startup Meiku fixes the directory owner to UID/GID 10001 when Docker permits CHOWN.

        Publish the WebUI through HTTPS if you want full PWA, Web Crypto and Service Worker support.
      de_de: |
        Required: set ISHIKU_SETUP_SECRET to a long random value before starting. DV2_SHARED_SECRET remains supported as a legacy alias.

        Persistent data is stored on the ZimaOS host at /DATA/AppData/meiku/data and mounted into the container at /data. On startup Meiku fixes the directory owner to UID/GID 10001 when Docker permits CHOWN.

        Publish the WebUI through HTTPS if you want full PWA, Web Crypto and Service Worker support.
